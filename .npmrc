# Workspace-level install settings. Workspace-local .npmrc files are ignored by npm.
puppeteer_skip_chromium_download=true
prefer-offline=true

# @nestjs/apollo 13 supports Apollo Server 5 but retains an unused Apollo 4
# playground plugin with an obsolete peer range. GraphiQL is configured instead.
legacy-peer-deps=true
