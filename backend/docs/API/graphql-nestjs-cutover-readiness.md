# GraphQL + NestJS cutover readiness

**Status:** Phase 0 baseline validated; Phase 1 contact, retained CSV transfer, tag, pipeline, deal, authenticated form-definition/submission-management, organization selector, authenticated calendar reads and definition mutations, canonical CRM persistence, retained public-form, workspace list/note/whiteboard mutations, and public-content realtime-revocation boundaries implemented; contact staging CRUD, bulk, activity, related-content, aggregate-profile, CSV transfer, organization-selector GraphQL/rollback, calendar list/detail/create/update/delete/availability GraphQL/rollback, workspace list/note/whiteboard read/mutation/realtime/rollback, and operation-observation gates completed

**Evidence date:** 2026-07-18
**Authority:** This document supersedes the testing counts and cutover-testing guidance in `ts-nest-rewrite.md`.

## Decision

The target public API is GraphQL on NestJS. Do not rebuild the complete REST surface in NestJS as a prerequisite. Extract transport-neutral domain services, expose them through GraphQL, and retain the legacy Express routes until each consumer scenario has passed a semantic cutover gate. A temporary NestJS REST adapter is appropriate only when a specific client cannot move with its GraphQL operation.

Parity means the same authorization decisions, domain state transitions, side effects, and user-visible outcomes. It does not mean forcing GraphQL responses and errors to imitate REST envelopes byte-for-byte.

## Verified baseline

The reproducible source inventory is [generated/rest-surface.md](generated/rest-surface.md). Run `npm run api:surface` after changing Express routes and `npm run api:surface:check` in CI.

| Surface | Verified snapshot | Interpretation |
| --- | ---: | --- |
| Express route declarations | 426 | Static declarations under `backend/src`, excluding tests and comment examples |
| Unique resolved method/path operations | 419 | Includes 412 `/api` operations and 7 GraphQL/documentation/health/fallback routes; this is the migration ledger input, not a claim that all operations are externally reachable |
| Backend test files | 84 | Includes database safety/bootstrap, contact-email and public-form normalization, aggregate-profile consumer ownership, workflow-registry drift and alias behavior, transactional trigger and realtime enqueue plus leased workers, webhook idempotency, tenant denial, the legacy-origin GraphQL and CSV-transfer proxies, request-correlation safety, shared GraphQL contracts, analytics parameters, realtime authorization, OAuth-state route/signing coverage, signature/file delivery, public-upload isolation, logo validation, CSV-transfer policy, public-sharing, audience-segment, campaign delivery, messaging delivery, ordered workflow rollout and staging safety, workflow execution/controlled egress, SMS provider ambiguity, and billing/scheduling concurrency tests |
| Real-database integration suites | 28 | 475/475 passed against a database built from zero, including the durable realtime outbox's rollback, idempotency, competing claims, retry, redacted dead-lettering, lease recovery, and live-room delivery; canonical contact-email, tag, pipeline-stage, and public-form migration repair/projection/tenancy; cross-channel email identity; typed public forms; live Socket.IO authorization/revocation/termination; analytics; selected-workspace persistence; invoicing; CRM; scheduling; sharing; workflow execution; and replay-safe provider boundaries |
| All legacy backend test cases | 801 | 326/326 non-database cases and 475/475 fresh-PostgreSQL cases passed on 2026-07-18 |
| NestJS GraphQL tests | 21 focused suites plus 9 PostgreSQL suites | 136/136 focused cases and 82/82 fresh-PostgreSQL cases pass, including authenticated calendar list/detail, definition create/update/delete, atomic availability replacement, date-override upsert/delete, canonical delete/booking lock ordering, the retained-create-versus-GraphQL-delete race, active-booking protection, tenancy, validation, CSRF, limits, and REST interoperability; organization selection/default repair and concurrent first-workspace creation; workspace list/whiteboard optimistic concurrency; owner/shared transactional projections; bounded shared-whiteboard refetch events; personalized REST conditional-cache rollback; fractional workspace geometry; default-category self-healing; and the transaction-bound realtime enqueue service |
| Frontend test files | 41 | 176/176 cases pass; independently flagged calendar read, definition-mutation, and availability-mutation mapping/default-off rollback plus the settings consumer, public-form defenses, shared organization selection and its independently flagged GraphQL adapter, public-content realtime revocation/recovery, separately flagged contact, pipeline, deal, form, onboarding, category, workspace-content list/note/whiteboard reads and mutations, Global Search envelope normalization, robust CSV parsing and bounds, GraphQL mutation CSRF, session recovery, and retained-shape mapping join the existing utility and feature coverage |
| Direct `{ error: ... }` JSON response sites | 300 | Evidence of an error-contract normalization problem, not necessarily 300 distinct shapes |

The 28 database integration suites cover the realtime outbox, Socket.IO authorization, analytics, automation execution, workflow trigger queueing, bookings, calendars, campaigns, contacts and CSV transfer, email templates, Resend email webhooks, Meta social webhooks, estimates, forms, invoice actions, invoices, lists, notes, organizations, pipelines, public sharing, audience segments, signatures, SMS messaging/webhooks, Stripe invoice and subscription webhook concurrency, tags, and workflows. Several suites include cross-organization or cross-owner denial scenarios, so isolation is not wholly untested. Coverage is still far smaller than the 412-operation `/api` surface and is concentrated in selected domains.

The former claim of 8 suites and 73 tests was stale. The current baseline was executed with the disposable runner on 2026-07-18: all 96 expected tables and 69 top-level migration markers were verified before all 28 suites and 475 tests passed.

## Phase 1 GraphQL foundation now in place

The isolated `backend-v2/` workspace now boots NestJS 11 with code-first GraphQL and Apollo Server 5. It is not part of the legacy Railway build/start commands and receives no production traffic.

The first vertical foundation implements the existing access-cookie contract, verifies `itemize_auth` with the shared JWT secret, selects organization context only from `x-organization-id` or the user's database default, re-reads current membership and role from PostgreSQL, and propagates the verified identity through `AsyncLocalStorage`. A public `readiness` query and protected `viewerContext` query prove the middleware and guard chain.

The first domain slice exposes tenant-scoped `contacts` and `contact` queries plus single-row and bounded bulk contact mutations through a transport-neutral service and parameterized repository. Reads preserve legacy search, status, tag, assignee, sorting, and page semantics while adding strict inputs and deterministic ID tie-breaking. Writes serialize plan-limit checks, verify assignee membership, use organization-qualified locks and writes, and commit contact-created/contact-updated/tag workflow triggers and activities atomically with domain state. Updates preserve omitted fields and clear nullable fields only when null or empty is explicit. Bulk requests are capped at 100 IDs, deduplicated in request order, mutate only matched active-organization rows, and return requested/matched/changed/rejected sets without revealing whether a rejected ID is foreign or absent. Cross-tenant single-row reads and mutations return tenant-private `NOT_FOUND`. The comparison suite also found and repaired ambiguous unqualified columns in the legacy contact search query.

The canonical tag persistence checkpoint now gives each organization one normalized tag identity and stores contact/deal membership in junction rows. A one-time repair migration merges case drift, preserves the union of array and junction membership, rejects cross-tenant links, and installs bidirectional compatibility projection so retained array writers and future ID-based writers cannot silently diverge. Tag rename preserves the ID and updates contact/deal arrays; delete removes all membership; campaign and segment evaluators immediately see tags supplied through legacy arrays. Focused fresh-PostgreSQL coverage proves all of these cases, and the disposable runner now forwards optional Jest path arguments for fast clean-schema checkpoint reruns.

The canonical pipeline-stage checkpoint makes `pipeline_stages` authoritative while retaining `pipelines.stages` as a bidirectional compatibility projection. Historical repair trusts live JSON for overlapping definitions, removes unused stale shadow rows, preserves deal-referenced missing keys, and repairs empty pipelines. Composite deal foreign keys enforce both tenant ownership and stage membership, a partial unique index protects default-pipeline concurrency, and core deal/segment validation reads canonical rows. Focused fresh-PostgreSQL coverage proves JSON and direct-row projection, bounds and duplicate rejection, in-use deletion denial, direct tenant/stage constraint failures, default uniqueness, and idempotent drift repair.

The authenticated vocabulary checkpoint adds `TagsModule` and `PipelinesModule` to NestJS. Their ten operations read and mutate the canonical tag/stage stores with CSRF-protected writes, tenant-private misses, deterministic ordering and aggregates, case-insensitive tag uniqueness, projection-preserving rename/delete, stage validation/reordering, explicit nullable-description clearing, serialized default changes, in-use stage protection, and locked deletion checks. The pipeline board adapter preserves the current consumer shape and has independent read/mutation rollback flags. Fresh PostgreSQL runs 6 tag/pipeline parity cases in addition to the 24 contact/context cases.

The deal checkpoint adds `DealsModule` with deterministic filtered reads and all seven CSRF-protected writes. It preserves decimal precision as strings, validates pipeline/stage/contact/assignee tuples, serializes concurrent updates, and atomically records real stage/lifecycle transitions in the deal ledger, contact timeline when applicable, and durable workflow queue. Database checks prohibit contradictory terminal state. Two independent default-off frontend flags preserve REST rollback without coupling deal transport to pipeline-definition flags.

The authenticated forms checkpoint adds nine `FormsModule` operations for definition CRUD/duplication, atomic field replacement, and submission paging/deletion. It keeps anonymous retrieval/submission on the hardened HTTP protocol. Reads expose real fields and deterministic counts/order; writes serialize plan-limit checks, distinguish omitted settings from explicit nullable clearing, validate publishing under a row lock, remap conditional field IDs transactionally, require CSRF, and return tenant-private misses. Three independent frontend flags preserve the existing consumer contract and leave public form methods untouched. Five fresh-PostgreSQL cases join focused contract and adapter tests.

The canonical contact-email checkpoint stores trimmed lowercase email or `NULL` at the database boundary while deliberately preserving duplicate and email-less contacts. Public forms and bookings resolve a canonical email only inside the active organization and deterministically reuse the lowest eligible contact ID. CSV import preserves its explicit duplicate toggle; campaign preview and snapshots count one recipient per canonical email. Fresh-PostgreSQL coverage proves migration repair, direct SQL and REST/GraphQL normalization, duplicate preservation, both CSV modes, concurrent form reuse, deterministic booking linkage, and campaign recipient deduplication.

The retained public-form checkpoint gives every form a database-generated globally unique public ID while allowing a legacy slug only when it identifies exactly one published form. Definition publishing and anonymous submission share a fail-closed typed contract for supported fields, options, safe patterns, numeric/string bounds, contact mappings, and conditional rules. Submissions are capped at 64 KiB and 100 fields, reject unknown keys, use a dedicated 60-per-15-minute limiter, and store only tenant-consistent JSON objects. Redirects are absolute credential-free HTTP(S) at route, database, and browser boundaries. The submission, canonical workflow trigger, and deduplicated notification intents commit together; the existing leased worker delivers notifications without embedding submitted values. The frontend now has a public renderer for every supported control and mirrors condition behavior. Focused unit, frontend, and fresh-PostgreSQL coverage proves the contract and replay-safe historical repair.

The retained CSV-transfer checkpoint adds `ContactTransfersModule` for authenticated `GET /api/contacts/export/csv` and CSRF-protected `POST /api/contacts/import/csv`. It caps imports at 1 MiB, 10,000 rows, 20 columns, and 100 returned row errors, caps exports at 50,000 rows, serializes duplicate and plan-limit decisions, neutralizes spreadsheet formulas, and records workflow/activity effects with the contacts transaction. The legacy origin can proxy only these two routes behind `CONTACT_TRANSFERS_NESTJS_ENABLED`; false or absent is an immediate REST rollback. The frontend parser now supports quoted commas/newlines, escaped quotes, CRLF, BOM and header aliases while rejecting malformed, oversized, over-wide, or over-row-limit files.

The authenticated calendar-read checkpoint adds `CalendarsModule` with tenant-scoped `calendars` and `calendar` queries. It keeps every calendar write, booking, public-booking, OAuth, provider-connection, and sync operation on the retained transports, while one default-off frontend flag selects GraphQL list/detail reads without changing consumer shapes. Fresh PostgreSQL proves selected-organization isolation, foreign-calendar concealment, REST interoperability, current-member assignee projection, confirmed-booking counts, recurring availability, and future-override filtering.

The NestJS build passes. Its 21 focused suites and 136 cases cover the foundation, auth/context/CSRF, organization selector, calendar reads, definition writes, availability replacement, and date overrides, contact/profile/CSV behavior, deal validation, form-definition validation, onboarding and workspace-content normalization/validation, repository transactions, section health, and observability. Aggregate-profile coverage independently injects failure into all nine child repositories and all nine service collection mappings. The disposable fresh runner executes 82 NestJS cases across 9 suites, including calendar list/detail, create/update/delete, atomic availability replacement, override upsert/delete, canonical retained-booking/delete lock ordering and their create/delete race, tenancy, CSRF, validation, limits, null/omission semantics, and bidirectional REST interoperability; organization membership/default projection; selection/REST interoperability; outsider denial; concurrent first-workspace creation; retained CSV; onboarding; categories; workspace reads; personalized REST conditional-cache rollback; list/whiteboard optimistic concurrency and realtime projections; fractional geometry; default-category self-healing; and list/note/whiteboard mutation scenarios. The contact adapter retains five flags, pipeline adds two, deals add two, forms adds three, onboarding adds independent read/write flags, organization selection adds independent read/write flags, calendar reads, definition mutations, and availability mutations add three independent flags, categories add two, and workspace content adds six; each preserves existing consumer shapes, obtains CSRF for writes, recovers once from `UNAUTHENTICATED`, and defaults to REST. Organization-scoped operations include verified organization context while the selector is user-global and independently verifies membership; onboarding and workspace content deliberately remain user-scoped. The CSV proxy flag is absent after its staging gate, all frontend GraphQL flags remain disabled, and no production traffic reaches NestJS.

## Staging cutover rehearsal

On 2026-07-16, an isolated `itemize.cloud GraphQL` Railway staging service was deployed beside the legacy staging backend against the existing staging PostgreSQL database. The legacy backend now owns a rate-limited `POST /graphql` endpoint and forwards only the authentication cookie, verified-organization header, request ID, and request body to the NestJS service over Railway's private network. Unsupported methods fail with a stable `405` GraphQL envelope; an unavailable or misconfigured upstream fails closed without falling through to legacy routes.

Wire checks passed for public readiness, exact credentialed CORS, hostile-origin denial, authenticated JWT and organization membership, contact pagination, private-network forwarding, and the REST rollback path. A local development harness exercised the real frontend against the staging legacy origin: `/contacts` rendered the GraphQL-backed list and `/contacts/1` rendered detail, with Railway logs recording `POST /graphql 200`; disabling the contact-read flag rendered the same journeys through `GET /api/contacts 200` and `GET /api/contacts/1 200`.

A second browser rehearsal used 55 temporary contacts plus the existing canary to prove GraphQL search, inactive-status filtering, and page navigation (`51`–`56` of `56`), then repeated search and pagination with REST rollback enabled. An eight-second access token exposed a client defect: GraphQL returned HTTP `200` with `UNAUTHENTICATED`, bypassing the Axios HTTP-401 refresh interceptor. The shared GraphQL client now performs one CSRF-protected `POST /api/auth/refresh`, retries the operation once, and fails closed if refresh is rejected; the browser replay passed and unit tests cover success and invalid-refresh behavior. Explicit organization headers returned zero matching rows in the default organization and the one isolation row in the second organization. Temporary contacts, membership, and organization were removed, returning staging to its original one contact and one organization.

The frontend now has one shared organization provider and a compact switcher that appears for multi-workspace memberships. `POST /api/organizations/:organizationId/select` re-verifies membership, persists the selection as the user's default, and returns the selected workspace; initialization repairs stale defaults, and switching clears tenant-scoped React Query caches before consumers reload. Four frontend tests cover selected membership initialization, switching/cache clearing, first-workspace creation, and stale-default repair; three new PostgreSQL cases cover persistence, outsider denial, and repair.

The GraphQL selector checkpoint exposes the three operations used by that provider: `organizations`, `selectOrganization`, and `ensureDefaultOrganization`. Reads and mutations have separate default-off flags, retained REST rollback, retained response-shape mapping, and CSRF-protected writes. The fresh runner proves list/default projection, GraphQL selection followed by a retained REST read, outsider and CSRF denial, and concurrency-safe personal-workspace creation. Nine unreferenced organization-management adapters remain REST and are not part of this checkpoint; destructive deletion remains blocked.

On 2026-07-18 the organization-selector operation gate passed against GraphQL deployment `e038d431-84cf-466b-98fc-039465f28f69` through backend deployment `77944478-e429-48e8-8fda-5837ea481ae0`. A disposable account with two memberships loaded Alpha through `Organizations`, switched to Beta through CSRF-protected `SelectOrganization`, and retained Beta after a full page reload; an authenticated `EnsureDefaultOrganization` probe returned the same selected membership on repeated calls. Proxy and NestJS operation events paired each successful request by request ID with zero GraphQL errors. With both selector flags disabled in the same frontend session, `GET /api/organizations` loaded the GraphQL-selected Beta, `POST /api/organizations/21/select` returned `200`, and a full reload retained Alpha through REST without data repair. Cleanup returned zero disposable users, organizations, or memberships. The localhost CORS allowance was deleted, clean backend deployment `8007e7a6-c4d0-4b53-b51f-f98077be43a1` restored the current source, and direct plus proxied `readiness` both returned `ready`. The selector flags remain default-off and production was untouched.

The completed calendar gate ran against GraphQL deployment `9705daa8-115f-4a44-acff-87919b3ee38a` through backend gate deployment `b7e0d641-4c1e-41f4-be13-f19c87ef84f5`. A disposable verified workspace created one distinctive calendar through retained REST, then a real navigation reload rendered the nonempty list through `CalendarReads` and the newly registered `/calendars/:id` settings page through `CalendarRead`, including all five default weekday windows. The page changed its name and recurring Monday start through retained REST settings and availability writes. Real Chromium exposed that time inputs needed the native input event to update React state; after the correction, a fresh REST detail reload retained `10:00`. With only `VITE_CALENDAR_READS_GRAPHQL` disabled, the same session and state rendered through `GET /api/calendars` and `GET /api/calendars/3` without data repair. Re-enabling the flag rendered the final persisted state through paired zero-error `CalendarReads` and `CalendarRead` events. Cleanup removed one disposable organization and account, stopped the harness, removed localhost CORS, and clean backend deployment `92940159-98ff-41a8-a2c9-306109530890` restored current source. The calendar flag remains default-off and production was untouched.

The calendar definition-mutation gate then passed against GraphQL deployment `4b6c8105-7364-40a8-98bd-98f0b251d490` through backend gate deployment `aa8a4f61-a8d4-4874-b6cb-d38dadf69370`. A disposable account registered and signed in through the real credential forms. With calendar reads and definition mutations enabled, the browser created a calendar through `createCalendar`, reloaded its GraphQL-backed detail, and updated its name, description, and duration from 30 to 45 minutes through `updateCalendar`; the visible list and settings state persisted, and Railway recorded successful browser-originated `POST /graphql` requests through the legacy-origin proxy. Disabling only `VITE_CALENDAR_MUTATIONS_GRAPHQL` retained the same session and calendar while reads stayed on GraphQL; the settings page renamed the same row again and changed its duration to 60 minutes through `PUT /api/calendars/4` (`200`) with no later GraphQL mutation request. Rollback required no data repair. Transactional cleanup removed the disposable user, organization, and calendar and verified zero remaining rows. The local harness stopped, temporary localhost CORS was removed, clean backend deployment `c8f9ee32-a5aa-4094-acb6-a1913fb31663` restored committed source, and direct plus proxied GraphQL readiness both returned `Query`. Both calendar flags remain default-off and production was untouched.

The calendar availability/date-override checkpoint adds atomic recurring-schedule replacement and strictly validated override upsert/delete behind independent default-off `VITE_CALENDAR_AVAILABILITY_MUTATIONS_GRAPHQL`. PostgreSQL coverage proves GraphQL-to-REST and REST-to-GraphQL interoperability, overlap failure without destructive replacement, foreign-calendar concealment, parent-scoped override deletion, and CSRF denial. The active weekly-availability settings writer passed a staging browser mutation, persisted GraphQL readback, and independent flag-off REST rollback against GraphQL deployment `2f0078e1-bccc-421b-acb0-f27055dce474`; proxy mutation request `448a3cbd-c6cb-477e-a929-644757bf98d1` and retained REST request `90acc609-6875-4cd2-a8c8-d58bc0872d09` both returned HTTP `200`. Cleanup returned zero fixture rows, clean backend deployment `a7addfc5-3a75-40b8-b434-848959ed01fb` removed temporary CORS and restored current source, and direct plus proxied GraphQL probes returned `Query`. The settings page currently displays date overrides but has no active override writer controls, so those adapter operations remain implementation/integration-proven rather than browser-gated. Bookings and public/provider protocols remain retained; the flag remains default-off and production was untouched.

Calendar deletion passed its independent real-browser GraphQL and flag-off REST rollback gate against GraphQL deployment `c6ef5954-5971-4eca-8bdb-c4858f0e8393`. A successful delete removed its row under request `e44afb26-6c7f-49c6-809e-db9df8e2a8d1`; a future-confirmed-booking delete remained visible and returned stable `UPCOMING_BOOKINGS` evidence under request `c6ae0343-d442-4e60-b112-cc04a993e732`; and disabling only `VITE_CALENDAR_MUTATIONS_GRAPHQL` deleted a separate calendar through retained `DELETE /api/calendars/8` (`200`, request `W1_sDqneQMWYN0PpnpoFkQ`) without repair. The UI currently reduces the actionable booking-conflict copy to a generic delete error, which remains a non-blocking presentation follow-up. Cleanup verified zero account, organization, membership, calendar, or booking fixture rows, removed localhost CORS, stopped the harness, and restored committed source in healthy backend deployment `8b238070-1085-4977-8b09-12761f8c3e4d`; direct and proxied GraphQL probes returned `Query`, flags remain default-off, and production was untouched.

On 2026-07-17 the legacy staging backend deployment `839c2af2-cbdd-48e3-8802-72a27a7d2d33` passed its health gate, and the real workspace selector was replayed against two memberships with one distinctive contact in each tenant. With GraphQL reads enabled, Alpha exposed only the Alpha fixture, selecting Beta produced `POST /api/organizations/3/select` followed by `POST /graphql`, Beta exposed only its fixture, and the Beta selection survived a full page reload. After switching back, the same journey passed with GraphQL reads disabled: the trace changed to tenant-scoped `GET /api/contacts?organization_id=1` and `organization_id=3`, and persisted selection again survived reload. The original workspace was restored before cleanup; two temporary contacts and the temporary organization were deleted, and a final database query found zero fixture rows.

The contact mutation gate then passed against GraphQL deployment `239de591-6f1a-4be7-b10a-08a59070cc15` through legacy backend deployment `095eb5e5-a5c4-4da4-94e8-686fb1e842f6`. A disposable staging user signed in through the real credential form. With both contact flags enabled, the browser created, edited, viewed, and deleted a contact through `POST /graphql`; visible success states and the detail activity were correct, and PostgreSQL recorded the expected `contact_added` and changed-field `contact_updated` workflow triggers. With reads still on GraphQL and only `VITE_CONTACT_MUTATIONS_GRAPHQL` disabled, the same browser flow created, edited, and deleted through REST (`POST 201`, `PUT 200`, `DELETE 200`). The rollback required no data repair. Cleanup removed the disposable user, organization, contacts, and triggers; the final query returned zero rows for every fixture category.

GraphQL deployment `f4c6c2e3-ea1e-402e-8abe-84e002654d85` and legacy backend deployment `4ca6b703-cb38-4161-a608-4cecee272856` then completed the synthetic operation-observation gate. One authenticated query, one unauthenticated query, one missing-CSRF mutation, and successful create/update/delete mutations produced six proxy events and six NestJS events. Every pair used the same request ID and operation name/type; `UNAUTHENTICATED` and `FORBIDDEN` produced operation-level errors despite HTTP 200; successful operations remained zero-error; and both layers emitted numeric duration. A privacy assertion found no fixture email, password, or contact field in the operation events. Local tests retain the proxy timeout/unavailable failure drill without disrupting the shared staging service. Cleanup again returned zero temporary users, organizations, contacts, and workflow triggers. Production was not deployed and both frontend flags remain off in deployed builds.

The bounded bulk-mutation gate then passed against GraphQL deployment `fd60f6b4-a1bc-440c-a7b4-e842d269ebba`. With contact reads and `VITE_CONTACT_BULK_MUTATIONS_GRAPHQL` enabled, the real browser selected three disposable contacts, added one tag to all three, and deleted one through `BulkUpdateContacts` and `BulkDeleteContacts`; the proxy recorded successful operation events and PostgreSQL moved from three contacts and six tag/update workflow triggers to two contacts with no contact activities. With only the bulk flag disabled, the same UI added a second tag to both survivors and deleted both through `POST /api/contacts/bulk-update` and `POST /api/contacts/bulk-delete`, both HTTP `200`, while reads remained on GraphQL. The rollback required no data repair. Cleanup returned zero disposable users, organizations, and contacts. The temporary localhost CORS allowance was removed, legacy backend deployment `331f20e8-88a9-404b-ae8b-4429e3fe38c1` restored the current source revision, and public `readiness` returned HTTP `200` with `ready`. Production was untouched and all then-existing frontend GraphQL flags remained off in deployed builds.

The activity gate then passed against GraphQL deployment `e7c7e836-9669-47bb-a4d4-dcfcec9467e8` through backend deployment `ff5cb00e-0bb6-4752-b402-b0f65e74eacb`. A disposable account registered and signed in through the real credential forms, created a contact through the retained REST mutation path, and opened its detail timeline. With `VITE_CONTACT_ACTIVITIES_GRAPHQL` enabled, the initial timeline read, a user-authored note mutation, and the refreshed read passed through `ContactActivities`, `AddContactActivity`, and `ContactActivities`; proxy and NestJS events paired all three successful operations by request ID. Disabling only the activity flag retained the same session and contact, loaded the GraphQL-created note through `GET /api/contacts/68/activities`, created a second note through `POST /api/contacts/68/activities` (`201`), and refreshed both notes through REST without data repair or later GraphQL activity traffic. Cleanup transactionally deleted one workspace and one user and reported zero residual users, organizations, contacts, or activities. The local harness stopped, temporary CORS was deleted, backend deployment `241377a4-feb7-49ae-9b06-7314836292d1` restored current source, and GraphQL deployment `6e1b6e2c-6195-4516-8091-205637c412a5` restored the clean committed source. Production was untouched and all four frontend GraphQL flags remain off in deployed builds.

The bounded related-content gate passed against GraphQL deployment `5426037f-c308-4f0e-977f-25f6e2d13623` through backend deployment `a954a6c2-e165-4611-9924-c24ca3dbb68e`. A disposable verified account signed in through the real credential form and created contact `69` through the retained REST mutation path. With `VITE_CONTACT_CONTENT_GRAPHQL` enabled, the contact-detail Related Content tab rendered one linked list, note, and whiteboard; the legacy-origin proxy recorded `ContactContent` request `5b268fd0-34fa-405c-80a4-d72b36f80abb` as HTTP `200`, zero-error success. Disabling only the content flag retained the same session and rows, rendered the identical three collections through `GET /api/contacts/69/content`, and Railway recorded HTTP `200` without a later `ContactContent` operation. Rollback required no data repair. Transactional cleanup deleted the disposable organization and account and reported zero remaining fixture accounts; the local harness stopped and temporary localhost CORS was removed. Backend deployment `a82a6554-0a4c-4021-80af-5a221ca4a834` restored clean current source. Production was untouched and all five frontend GraphQL flags remain off in deployed builds.

The aggregate-profile operation gate then passed against GraphQL deployment `95142285-8705-49c6-b6ac-781230d8bf6e` through the existing clean backend deployment. Two disposable verified workspaces and one complete profile fixture exercised invoices, signatures, payments, activities, notes, lists, communications, tasks, and bookings through the authenticated legacy origin. REST and GraphQL both returned HTTP `200`; GraphQL request `39666ef1-4b7e-49af-8c4f-d5ed22e5c3ac` reported every section `AVAILABLE`, total `1`, and no truncation. The retained REST aggregate reproduced its known masked failures by returning empty payments and lists, while GraphQL returned both current-schema rows. Outsider request `0bebcf0d-1360-4b31-adef-f056b2f1008f` returned operation-level `NOT_FOUND`. Cleanup deleted both disposable organizations and reported zero remaining fixture users. GraphQL readiness remained `ready`, no local server or temporary CORS was used, and production was untouched.

The aggregate-profile follow-up closes the remaining failure/consumer evidence. A focused matrix independently fails invoices, signatures, payments, activities, notes, lists, communications, tasks, and bookings at the repository and service layers, proving that one unavailable child cannot hide or relabel the other eight. A backend contract test reads the generated ledger and scans non-test frontend source: the legacy aggregate route has zero consumers and no shipped code references the `contactProfile` field. The current detail page intentionally uses narrower independently gated operations, so no aggregate-profile browser flag or rehearsal is warranted until a real consumer is introduced.

The retained CSV transport gate then passed against GraphQL deployment `0e3e01c8-fec1-4d7e-a3c5-6ab94c9e6712`. Backend deployment `3d2ade6e-d88d-4a1e-9a2c-6e8f52e46dbf` enabled only the two CSV proxy routes at the public legacy origin. A disposable verified account proved NestJS import success, canonical duplicate skipping, bounded row errors, tenant-filtered export, quoting, formula protection, no-store headers, and transactional trigger/activity effects. Removing the proxy flag and deploying current backend source as `193ab513-f74e-4565-8b9c-a7e3a8dab863` retained the same session and data: legacy export read the Nest-created row, legacy import added a second row with its distinct response contract, and the next legacy export read both without repair. Cleanup removed the disposable tenant and user and left zero contacts or triggers. Staging is back on legacy CSV routing, production was untouched, and `backend-v2/railway.json` now isolates GraphQL packaging from the root frontend manifest.

The independent workspace-read gate passed against GraphQL deployment `1bb70077-4237-406f-ad33-1e115a79a5ea` through backend deployment `07a810a1-5979-4508-9f12-317c12ab9d64`. A disposable verified account created one distinctive list and note through retained writes. With only list reads on GraphQL, both Canvas and Contents rendered both fixtures while logs recorded `WorkspaceLists` and REST `GET /api/notes`; after inverting the flags, both pages rendered the same rows while logs recorded `WorkspaceNotes` and REST `GET /api/canvas/lists`. With both flags disabled, the same pages rendered both fixtures through only the two REST reads, without a later GraphQL workspace operation or data repair. The gate also found that the retained `UserHome` `/lists` adapter is dead source in the active app router; the generated ledger now records that its static API callsite is not a shipped consumer. Cleanup returned zero fixture users, organizations, memberships, lists, notes, categories, or outbox rows. Temporary CORS was removed, clean backend deployment `fd5f06f0-70eb-4573-9eee-6141e42e6c8d` and GraphQL deployment `296c3ac3-faf0-476c-a502-d46788c11f08` restore default staging configuration, all workspace flags remain default-off, and production was untouched.

The workspace-list mutation gate then passed against GraphQL deployment `67a436eb-5175-48bc-b1ad-9c5eab74a5ac`, using backend deployment `c2c8eb70-c744-4939-827f-a2ac930e3b99` for the GraphQL UI phase and `afd0a162-e2c6-4b5e-bb66-e92b485aee38` for corrected REST rollback. The real Canvas created a default-category list, added and completed an item, shared it, and changed its title; Contents rendered the same row and an open anonymous viewer received live updates. A stale whole-list revision failed with `CONFLICT` and did not overwrite the newer state, while the transactional outbox delivered three shared-viewer and four owner-canvas projections. Flag-off rollback exposed a retained-ETag defect in personalized REST reads: Express could return `304` with no Axios body. The three workspace reads now use `private, no-store` and ignore conditional validators, with fresh-PostgreSQL coverage requiring complete `200` bodies when ETags are replayed. REST update/delete then rendered correctly and reached the open viewer without a later GraphQL workspace operation. Cleanup returned zero fixture rows, temporary variables were removed, clean backend deployment `e402baa8-6154-4367-aa6d-a560737ad766` succeeded, GraphQL readiness returned `ready`, all workspace flags remain default-off, and production was untouched.

The workspace-note mutation gate passed against GraphQL deployment `d94ce176-cb73-43aa-ac46-58a430f59a30` through backend deployment `535b0cd4-5e41-4751-a135-15007afcbdb7`. A disposable verified account used the real canvas with only the note-mutation flag enabled to create at fractional coordinates, update title and rich content, create without any pre-existing category rows, and delete. An already-open anonymous shared-note tab received both updates and the deletion over Socket.IO without reload. The rehearsal found and fixed integer-only GraphQL geometry, the missing default `General` category on new accounts, and cross-tab CSRF-cookie rotation. Disabling only the mutation flag preserved the same session and rows while title/content updates and deletion passed through `PUT`/`DELETE /api/notes`; reload proved persistence and rollback required no repair. Logs separately recorded successful GraphQL operations, leased outbox deliveries, and REST writes. Transactional cleanup returned zero disposable users, organizations, memberships, categories, notes, and note outbox rows. Temporary CORS and worker variables were removed; clean backend deployment `7f9cce4e-c9e2-45ad-94da-517dad9e27d6` and GraphQL deployment `b8a0546e-96b6-4ec8-82af-0ad1b100bbba` succeeded, GraphQL readiness returned `ready`, all workspace flags and the worker remain default-off, and production was untouched.

The workspace-whiteboard gate passed against GraphQL deployment
`7eba9f77-e9e8-4f4a-bac0-3127267efc4f` through backend deployment
`3771549e-20fa-43c1-ac95-7cdbbb376d89`. A disposable verified account used
the real Canvas, Contents, Global Search, and anonymous shared viewer with
both whiteboard flags enabled. GraphQL create/update passed and the already-open
viewer applied the bounded refetch projection without a reload. The rehearsal
found that Global Search accepted only a bare array instead of the established
paginated whiteboard envelope; the consumer and regression coverage now
accept both. It also found migration `029` absent because production-mode
startup deliberately skips migration execution. The migration was explicitly
applied and verified in staging, and the startup health guard now requires its
marker. With both flags disabled, the same row rendered and searched through
REST; REST update reached the open viewer and REST deletion rendered the
terminal unavailable state without repair. Cleanup returned zero fixture and
outbox rows. Temporary variables are absent, clean backend deployment
`3f180b58-1988-42c5-87be-b187e4c4b924` and GraphQL deployment
`6e5ea4b8-6e9a-4ed8-bc2b-296345188055` succeeded, all workspace flags remain
default-off, and production was untouched.

The activity rehearsal also exposed an optional-field contract defect in the retained contact-create path: browser forms represented blank optional values as empty strings, while frontend and REST validation treated optional email and phone fields as valid only when omitted. Contact form validation now removes blank and whitespace-only optional strings before transport, the REST create/update validators accept blank optional email and phone values while still rejecting malformed non-empty values, and focused frontend/backend regressions cover both behaviors.

The earlier CRUD rehearsal also exposed two frontend defects outside the GraphQL transport: the shared Axios interceptor unwrapped the successful registration envelope before `AuthContext` checked its `success` property, and a structured REST error object could be passed directly to the React toast description. Registration now treats any resolved 2xx request as success, API-error extraction always returns text, and the toast renderer has a final renderability guard. Regression tests cover the data-only registration response, nested API errors, valid React toast content, and unknown-object fallback.

## Phase 0 work now in place

1. **Database blast-radius guard.** Integration tests require `TEST_DATABASE_URL`; they never fall back to `DATABASE_URL`, reject an identical URL, and reject ambiguously named databases unless isolation is explicitly acknowledged. Configuration starts from `backend/.env.test.example`.
2. **Reproducible REST inventory.** `scripts/generate-api-surface.js` statically resolves mounted routers without booting the app, connecting to PostgreSQL, or initializing external services. It produces JSON for tooling and Markdown for review.
3. **Drift checks.** `npm run api:surface:check` fails when the committed baseline is stale. `npm --prefix backend run test:integration:config` verifies the database guard without opening a database connection.
4. **Fresh database runner.** `npm run test:integration:fresh` in `backend/` creates PostgreSQL 16 with Docker, resets the guarded schema, uses the real application initializer, verifies 96 expected tables and 69 migration markers, disables inherited email/SMS/storage/payment/AI provider credentials, runs the integration suites, and destroys the database. The complete gate passed on 2026-07-18. See [Disposable integration database](../Tests/integration-database.md).
5. **Strict grouped migrations.** A grouped migration that reports internal failures is no longer recorded as successful.
6. **Stripe webhook idempotency.** Invoice webhook processing now claims a unique Stripe event inside the same transaction as payment/invoice mutations, serializes by payment reference, rejects a previously recorded payment reference, locks the invoice row, and rolls back the claim on failure. Both the application initializer and numbered production migration stream create the event table; production startup requires migration `006_stripe_webhook_idempotency`.
7. **REST-to-GraphQL cutover ledger.** `npm run api:ledger` correlates all 419 registered operations (412 under `/api`) with static frontend callsites and backend test references, recommends initial transports, assigns evidence-based risk, and exposes review queues. Human decisions and acknowledged dynamic helpers live in `graphql-operation-overrides.json`; see [GraphQL cutover ledger workflow](graphql-cutover-ledger-workflow.md).
8. **Frontend-route reconciliation.** All 18 previously unmatched frontend calls are resolved: incorrect public booking/form and search paths were fixed, missing invoice-payment and reputation-delete handlers were implemented, onboarding reset was made statically traceable, and unused legacy API wrappers were removed. The ledger now has zero unmatched calls and zero unreviewed runtime expressions.
9. **Authentication slice assignment and contract.** All 14 auth operations have owners, target `AuthModule` operations or retained-HTTP decisions, and parity states. The [authentication cutover contract](contracts/auth-graphql-cutover.md) freezes cookies, CSRF, identity, tenancy context, errors, transaction boundaries, and required scenarios. The active Google flow now validates provider tokens server-side instead of trusting browser-supplied identity fields.
10. **Tenancy context and organization slice.** Organization operations are assigned to `OrganizationsModule`, and the [tenancy contract](contracts/tenancy-graphql-context.md) freezes request-context selection, the role matrix, error semantics, transaction boundaries, and row-level SQL requirements. The three active selector consumers are implemented behind independent default-off flags; direct middleware and fresh-PostgreSQL tests cover explicit/default membership, outsider and role denial, invalid IDs, missing auth, optional context, connection cleanup, selected-default persistence, CSRF, REST interoperability, and concurrent first-workspace creation. The other nine management adapters have no shipped imports and remain on REST; destructive deletion remains blocked.
11. **Shared GraphQL transport contract.** The [shared contract](contracts/graphql-shared-contracts.md) freezes stable error codes, strict page-based pagination, scalar transport rules, nullability, and mutation-result conventions. Framework-neutral helpers and 20 tests prove normalization, internal-error redaction, page metadata, and precision-preserving decimal serialization.
12. **Invoicing slice assignment and contract.** All 46 invoice operations are assigned across eight billing modules with named GraphQL targets, retained-HTTP exceptions, risk, and parity status. The [invoicing contract](contracts/invoices-graphql-cutover.md) freezes decimal and currency boundaries, numbering, state transitions, transaction/locking rules, side-effect retry behavior, and the required parity matrix.
13. **Billing concurrency primitives.** Direct invoice creation, estimate conversion, manual recurring generation, and scheduled recurring generation now share an atomic PostgreSQL invoice-number allocator. Estimate numbering takes an organization-scoped transaction advisory lock; conversion locks its source; recurring routes lock tenant-owned templates; and background runners claim due templates with `FOR UPDATE ... SKIP LOCKED`. Manual invoice payments also lock their invoice so simultaneous payments accumulate without a lost update, and invalid payment-method values return a client error instead of leaking a constraint failure as HTTP 500. Focused query tests and real-PostgreSQL concurrent scenarios all pass.
14. **Campaigns and workflows slice.** All 33 campaign, workflow, enrollment, workflow-side-effect, and workflow-webhook operations have owners and named targets across `CampaignsModule`, `CampaignDeliveryModule`, `WorkflowsModule`, `WorkflowEnrollmentsModule`, `WorkflowExecutionModule`, and retained `WorkflowWebhooksModule` HTTP handling. The [campaigns/workflows contract](contracts/campaigns-workflows-graphql-cutover.md) freezes state machines, targeting, tenant scope, enrollment concurrency/lifecycle, execution observability, HMAC/replay behavior, side-effect boundaries, and required parity scenarios. Campaign list envelope mapping, send locking, pause-safe completion, cross-tenant enrollment cancellation, manual/deactivation pause separation, same-step retry, payload-free execution metrics/queue queries, dead-letter operator retry, SMS reconciliation, enum validation, simultaneous enrollment, and durable webhook claims now have executable coverage.
15. **Messaging slice.** All 26 email-template, SMS-template, administrator-email, contact-delivery, and Twilio-webhook operations have owners and named targets across `EmailTemplatesModule`, `SmsTemplatesModule`, `MessagingDeliveryModule`, `AdminMessagingModule`, and retained `SmsWebhooksModule` handlers. The [messaging contract](contracts/messaging-graphql-cutover.md) freezes rendering, tenant scope, delivery states, provider boundaries, admin batching, and webhook verification/replay behavior. Clean-schema activity/log writes, provider-unavailable semantics, globally unique organization-owned receiving numbers, tenant-first inbound routing, concurrent replay, unmatched/ambiguous quarantine, status replay, and durable webhook claims now have PostgreSQL coverage.
16. **Workflow execution trigger, enrollment, lifecycle, provider, egress, visibility, SMS-reconciliation, and rollout boundaries.** The [execution contract](contracts/workflow-execution-graphql-cutover.md) defines the durable worker target. `workflow-registry.json` supplies one trigger/step vocabulary to backend validation, database constraints, producers, frontend types/options/labels, and templates; a test keeps the deployable backend copy synchronized with the monorepo authority. All canonical automatic producers enqueue into one transactionally durable queue: contact create/update, actual tag add/remove, deal stage, form submission, booking create/cancel/reschedule, invoice paid, linked-contact contract completion, and explicit one-shot schedules. The retained signed webhook uses the same queue and returns `execution: durably_queued`. A `SKIP LOCKED` worker leases events, atomically matches workflows and activates due enrollments, while a separate opt-in scheduler executes those enrollments. Both phases default off for controlled rollout. Production scheduling now invokes one flag-aware ordered cycle: due schedules, trigger fan-out, enrollment execution, then provider delivery. Provider steps snapshot one durable intent per enrollment run/step; a separate leased worker retries with bounded delay and redacted dead letters, passes stable keys where providers support them, and writes correlated logs only after acceptance. Lifecycle and dead-letter retry policies are durable. Outbound webhooks use DNS-pinned public-only egress. Tenant-scoped summary and queue queries expose ages, attempts, failures, cancellations, dead letters, reconciliation state, retry history, and provider correlation IDs without payload leakage. Expired in-flight SMS attempts stop for accepted-SID or explicit-resend operator reconciliation instead of automatically duplicating Twilio I/O. Fresh PostgreSQL proves a real contact API mutation reaches one provider-isolated accepted delivery through all enabled phases and that a second cycle is a no-op. On 2026-07-16 the deployed Railway staging gate passed migration/backlog preflight, one ID-scoped Resend test delivery with correlated provider evidence, automatic fixture retirement, and a confirmed zero-residual disable-and-drain rehearsal. Formal alert routing and named production operational owners remain release actions.
17. **CRM slice.** All 43 contact, activity, tag, pipeline, deal, form, submission, CSV-transfer, and public-form operations have owners and targets across eight CRM modules. The [CRM contract](contracts/crm-graphql-cutover.md) freezes tenancy, aggregate PII, plan limits, bulk semantics, tag/stage model conflicts, deal references/lifecycle, form transactions, and retained public HTTP behavior. The unauthenticated aggregate contact-profile exposure and stale schema mapping are fixed; fresh PostgreSQL now covers forged tenant headers, assignment/reference denial, bulk tag semantics, plan/default/tag races, stage integrity, same-email public submissions, globally unambiguous form identity, typed submission validation, safe redirects, and durable notifications.
18. **Scheduling slice.** All 25 calendar, availability, booking, public-booking, OAuth, connection, and sync operations have named GraphQL or retained-HTTP targets across five modules. The [scheduling contract](contracts/scheduling-graphql-cutover.md) freezes interval/timezone rules, collision locking, lifecycle, public capability behavior, signed OAuth state, provider-secret handling, sync jobs, and durable booking events. Authenticated calendar list/detail is browser-gated behind its default-off flag with selected-tenant isolation, REST interoperability, assignee membership projection, booking aggregates, availability, and future overrides. `createCalendar`, `updateCalendar`, and `deleteCalendar` are browser-gated behind a second default-off flag with transactional plan-limit and membership checks, strict settings/window validation, CSRF, retained-shape mapping, omission/null semantics, independent REST rollback, canonical advisory-lock-first ordering shared by retained booking writes, a post-lock calendar recheck, concurrent create/delete coverage, foreign-tenant concealment, active future-booking protection, and cascade cleanup. Atomic availability replacement is browser-gated behind a third default-off flag with independent REST rollback, while strictly validated parent-tenant-scoped override upsert/delete remain implementation/integration-proven because the shipped page has no writer controls. Bookings and public/provider protocols remain retained. Google OAuth state is signed, expiring, membership-checked, and redirect-safe. Fresh PostgreSQL proves simultaneous same-slot exclusion and cancellation-token binding/replay behavior.
19. **E-signatures slice.** All 28 document, template, lifecycle, delivery, audit, file, and public-signing operations have named GraphQL or retained-HTTP targets across five modules. The [e-signatures contract](contracts/esignatures-graphql-cutover.md) freezes tenant/reference boundaries, draft immutability, lifecycle/concurrency, hashed capabilities, signer assurance, evidence, storage, and job boundaries. Legacy cancellation now actually transitions state and revokes tokens, sends serialize, reminders preserve terminal recipients and enforce tenancy, unsupported OTP assurance fails closed, unknown fields are rejected, and local file paths use a traversal-safe upload root.
20. **Public-sharing slice.** All 14 list, note, whiteboard, wireframe, and vault share operations have named GraphQL or retained-HTTP targets across three modules. The [public-sharing contract](contracts/sharing-graphql-cutover.md) freezes personal ownership, capability issuance/rotation/revocation, public projections, cache/privacy headers, sanitization, vault-secret semantics, realtime revocation, and the missing wireframe viewer. Legacy issuance is now concurrency-safe, revoke clears tokens, re-share rotates them, nested content is sanitized, public reads are no-store/noindex, the vault frontend consumes the actual response envelope, and list/note/whiteboard/wireframe revocation immediately evicts active Socket.IO viewers and clears shipped public-page state. The three shipped public viewers now reauthorize and refetch before applying queued reconnect updates, including fail-closed handling when a capability was revoked while offline.
21. **Audience-segment slice.** All 9 segment operations are assigned to `SegmentsModule`. The [audience-segment contract](contracts/segments-graphql-cutover.md) freezes the typed dynamic/static definition, fail-closed operator vocabulary, tenant references, parameterized evaluator, calculation/history concurrency, pagination, and saved campaign targeting. Legacy preview/count/membership/campaign preview/send now share the same evaluator; campaign `segment_id` is persisted by foreign key and recipient snapshots no longer broaden to every eligible contact.
22. **Analytics and dashboard slice.** All 9 analytics operations are assigned to `AnalyticsModule`. The [analytics contract](contracts/analytics-graphql-cutover.md) freezes strict period inputs, tenant and numeric semantics, selected-pipeline funnels, lifecycle communication counts, bucket identity, database-client serialization, and required parity scenarios. PostgreSQL coverage now proves the fixed mechanics; revenue recognition/currency, contact lifecycle, reporting timezone, and true stage-transition velocity remain explicit blockers rather than accidental legacy behavior.
23. **Realtime and Socket.IO slice.** The [realtime contract](contracts/realtime-socketio-cutover.md) freezes cookie-only private-room authentication, public/chat capability rules, organization chat/social separation, event envelopes, reconnect behavior, and the decision to retain Socket.IO during the GraphQL cutover. Legacy joins now validate inputs and database state, organization rooms enforce membership, typing spoof attempts fail closed, raw capabilities are removed from logs, browser clients rejoin with credentialed handshakes, database clients no longer leak, and successful public-content revocation emits a token-free event before adapter-wide room eviction. Ending a chat session now emits an equivalent token-free visitor event, evicts active visitors, serializes concurrent visitor/agent message writes on the active session row, denies subsequent writes, and prevents transcript reads from restoring online presence. `RealtimeOutboxModule` and migration 028 now provide a transactional, idempotent, leased PostgreSQL handoff from Nest mutations to the legacy socket host. Competing claims, rollback, retry, expired-lease recovery, and real-room delivery are covered; the worker remains opt-in and production remains untouched. Shared-adapter fan-out/presence remains the multi-instance blocker.
24. **File, binary, and bulk-transfer slice.** The [file/binary contract](contracts/files-binary-graphql-cutover.md) keeps multipart uploads, private PDF streams, generated PDFs, public logo bytes, and CSV transfer outside GraphQL while freezing their shared auth, tenant, storage, header, filename, size, and lifecycle rules. Signature uploads now validate PDF bytes and draft/template tenancy, private signature files are no longer exposed by the static upload mount, delivery refuses arbitrary remote URLs and unsafe local/S3 paths, logo writes validate actual image bytes and own their storage URL, and the NestJS retained CSV module enforces formula safety, strict validation, bounded imports/exports/errors, transactional effects, and a default-off legacy-origin rollback proxy. Focused non-database coverage plus six fresh-PostgreSQL contact-transfer scenarios cover this boundary. Malware/structural parsing, private-bucket policy, byte ranges, staged cleanup, evidence retention, and multi-instance storage remain explicit blockers.
25. **Provider webhook slice.** The [provider webhook contract](contracts/provider-webhooks-graphql-cutover.md) inventories Stripe invoice/subscription, Resend, Twilio, Meta, workflow, and Google OAuth callback protocols and freezes raw-body verification, durable delivery claims, occurrence-time ordering, acknowledgement, reconciliation, tenant mapping, and redaction. Resend verifies real Svix signatures, deduplicates by delivery ID, prevents out-of-order status regression, updates email/campaign milestones and contact suppression, and leases pending events while refusing cross-tenant provider-ID ambiguity. Meta verifies exact-body HMAC signatures, durably claims complete normalized batches, bounds inline work, drains overflow with leased workers, reconciles unmatched/ambiguous mappings without tenant guessing, and emits sockets only after commit. Twilio inbound callbacks select tenancy from an organization-owned receiving number before matching the sender inside that tenant. Stripe subscription callbacks add minimal replay snapshots, deterministic same-second ordering, leased tenant reconciliation, and idempotent upgrade notifications. Workflow provider intents share a leased outbox, outbound webhooks use controlled public-only pinned egress, and ambiguous outbound SMS attempts have tenant-scoped reconciliation controls.
26. **Onboarding slice.** All 6 authenticated onboarding operations are assigned to `OnboardingModule` and have passed dual REST/GraphQL parity. The [onboarding contract](contracts/onboarding-graphql-cutover.md) freezes user scope across workspace changes, key/version/step validation, deterministic typed progress, CSRF, row-locked concurrent updates, atomic analytics events, reset semantics, and independent read/write rollback flags. Fresh PostgreSQL proves the lifecycle, durable events, concurrency, user isolation, validation, and CSRF; frontend mapping and REST-default tests cover all six consumers.
27. **Workspace content slice.** Personal list/note/whiteboard reads and reachable CRUD are assigned to `WorkspaceContentModule`. The [workspace content contract](contracts/workspace-content-graphql-cutover.md) freezes user scope, paging/search, category identity, bounded list items and whiteboard JSON, geometry, row locking, optimistic whole-list/whiteboard revisions, CSRF, REST rollback compatibility, personalized read cache behavior, and transactional owner/shared publication. Six separate default-off read/mutation flags preserve independent rollback. Fresh PostgreSQL proves all three domains' CRUD, tenant-private misses, stale-list/whiteboard conflict rejection, concurrent disjoint note updates, category projection, full REST `200` reads when conditional validators are replayed, REST reads of GraphQL writes, atomic realtime enqueue, bounded whiteboard refetch events, legacy socket-worker delivery, and delete propagation. Canvas, Contents, Global Search, and the anonymous shared viewers passed staging GraphQL and REST-rollback gates for all three domains. Active mixed-aggregate position batching stays on retained HTTP, while dead dedicated position/item adapters remain blocked.

## P0 implementations verified against PostgreSQL

### 1. Reproducible disposable database provisioning

The fresh runner and schema contract are implemented and passed locally on 2026-07-15. Clean initialization also proved that required migration failures stop startup and that onboarding, email-authentication, invoicing/estimates, and landing-page dependencies are created in a valid order.

**Gate result:** `npm run test:integration:fresh` passed with 96 tables, 69 markers, 28 legacy suites and 475 legacy tests, followed by 9 NestJS suites and 74 tests. CI should run the same command; no shared long-lived database is used.

### 2. Stripe invoice webhook idempotency

The legacy handler now has transactional event claims and payment-reference deduplication, with unit/route tests for duplicate delivery, duplicate payment references, commit, and rollback.

**Gate result:** the concurrent-delivery scenario passed against fresh PostgreSQL. Preserve this behavior as the NestJS semantic contract.

## Remaining P0 blockers

### 3. REST operation to GraphQL operation ledger

The generated ledger now supplies an initial disposition and evidence for every REST operation, and its unmatched frontend queue is empty. Human decisions now cover 268 authentication, organization, invoicing, campaign, workflow, enrollment, messaging, CRM, scheduling, e-signature, file-transfer, public-sharing, audience-segment, analytics, onboarding, and retained-protocol operations. Owners still need to approve the recommendations and name targets for the remaining domains.

The ledger should record:

- REST method/path and source location
- frontend and external consumers
- required role and tenant boundary
- input, validation, and nullability rules
- domain state changes and transaction boundary
- external side effects and idempotency key
- GraphQL operation/field and owning NestJS module
- parity scenarios and cutover status

Webhook receivers, OAuth callbacks, health checks, downloads, and some uploads normally remain HTTP endpoints; GraphQL is not a reason to force non-GraphQL protocols into the schema.

### 4. Frozen cross-cutting contracts

Authentication, tenancy, shared error/pagination/scalar rules, invoicing, campaigns, workflows, enrollments, messaging delivery, CRM, scheduling, e-signatures, file/binary transfer, public sharing, audience segments, analytics, and their retained webhook/public/binary/OAuth protocols are now documented in dedicated contracts. Before implementing the remaining slices, document and test the observable behavior of:

- field-specific ordering, filtering, search, and null semantics not covered by the shared transport rules
- file malware/structural validation, private-bucket policy, byte ranges, staged cleanup, evidence retention, and multi-instance storage behavior
- provider dead-letter operator controls and the remaining provider retry/transaction boundaries in the provider contract
- remaining realtime multi-instance fan-out/presence and mutation-by-mutation
  outbox adoption

Socket.IO now has a dedicated contract plus unit, live PostgreSQL protocol, and real-browser coverage. Public-content revocation uses adapter-aware room lookup/emit/leave operations and clears the three reachable public viewers immediately. Their reusable recovery gate reauthorizes and refetches before applying queued reconnect updates, clears stale content when a capability was revoked offline, and preserves last-loaded content in an explicitly offline state when recovery fails. Chat-session termination now uses the same adapter-aware eviction boundary, denies post-end visitor and agent writes, and keeps retained transcript reads from reviving presence. The transactional realtime outbox gives separate Nest mutations a durable at-least-once handoff to the current socket host with idempotent keys, bounded payloads, attempt fencing, retries, and dead letters; it deliberately does not claim client receipts or replay cursors. `npm run test:browser:shared-realtime` continues to prove authoritative recovery. Viewer count is protocol telemetry, not a supported shared-page UI feature. A deployed shared adapter with two-instance fan-out/presence proof is still required before multi-instance production cutover.

Workflow execution has a dedicated contract and its code-level and deployed-staging rollout gates are complete. Every canonical automatic producer writes the shared queue, scheduled workflows have an explicit one-shot contract, trigger events and provider delivery are durably queued and leased, due enrollment steps use persisted attempt/token fencing, lifecycle/dead-letter policies are durable, outbound webhooks use public-only DNS-pinned egress, tenant-scoped operator queries expose queue and failure metrics without payload leakage, ambiguous Twilio attempts stop for explicit reconciliation, and an ordered enabled cycle is proven end to end from contact API mutation through provider acceptance. The staging harness passed migration/configuration/backlog preflight, an ID-scoped Resend test-address canary, JSON evidence capture, fixture retirement, and a confirmed global disable-and-drain rehearsal on 2026-07-16. Remaining release work is formal alert routing, named dead-letter/SMS-reconciliation owners, and the production change window.

### 5. Consumer-level verification

Forty-one frontend test files and 172 cases include independently flagged calendar read and definition-mutation mapping and transport selection, public-form defenses, public-content realtime revocation and reconnect recovery, organization selector reads/writes, contact read/write/bulk/activity/content coverage, RFC-style CSV parsing and file bounds, pipeline/deal mapping, authenticated-form read/mutation/submission mapping, onboarding, categories, workspace list/note/whiteboard reads, all three domains' reachable mutations, and Global Search normalization of both whiteboard response shapes. They prove REST-default rollback, independent GraphQL selection, user-versus-organization scope, organization headers, CSRF transport, retained response shapes, list/whiteboard revision preservation, serialized whiteboard saves, fail-closed missing-revision handling, granular note-write mapping, revoked-content removal, reconnect/refetch ordering, revoked-while-offline denial, failed-recovery fallback, nullable clearing, conditional-field variables, submission pagination, session recovery, and organization-provider behavior. The separate real-Chromium gate passes four shared-page disruption/lifecycle scenarios without using a cloud service or persistent database. Deployed GraphQL flags remain disabled and retained CSV routing is back on legacy.

## Required test layers

| Layer | What it proves | Cutover requirement |
| --- | --- | --- |
| Domain service unit tests | Business rules independent of Express, NestJS, or GraphQL | Required for extracted services and high-risk calculations/state machines |
| PostgreSQL integration tests | SQL, constraints, transactions, tenancy, and migrations | Run against a database built from zero |
| Legacy REST characterization tests | Current observable semantics for still-supported behavior | Required per ledger scenario before reimplementation |
| GraphQL resolver/operation tests | Schema inputs, auth, nullability, error codes, and output shape | Required for every target query/mutation |
| Dual semantic parity tests | Equivalent authorization, normalized domain state, and side effects | Required per migrated scenario; do not compare transport envelopes literally |
| Protocol contract tests | Webhooks, OAuth callbacks, uploads/downloads, health, Socket.IO | Required before changing the owning implementation |
| Frontend/browser tests | Real consumer behavior through the new API | Required for critical user journeys before traffic cutover |
| Operational tests | Logging, correlation IDs, rate limits, timeouts, shutdown, observability | Required before production traffic |

## Recommended sequence

1. Keep `npm run test:integration:fresh` as a required CI gate so clean bootstrap and all 28 database suites remain continuously proven.
2. Continue assigning owners and GraphQL targets after the 268 authentication, organization, invoicing, campaign, workflow, enrollment, messaging, CRM, scheduling, e-signature, file-transfer, public-sharing, audience-segment, analytics, onboarding, and retained-protocol operations already decided, prioritizing frontend-consumed high-risk operations.
3. Freeze the remaining field-specific query and provider-webhook contracts; implement the documented file-storage and realtime shared-adapter gates, and adopt the durable outbox in each migrated mutation.
4. Define the GraphQL schema and NestJS module boundaries around domain behavior, not legacy route-file boundaries.
5. Extract or reimplement transport-neutral services and add PostgreSQL integration tests.
6. Migrate vertical slices: GraphQL operation, consumer change, semantic parity, observability, then retire the corresponding REST consumer path.
7. Cut over only after critical browser journeys and rollback behavior pass in a production-like environment.

The implemented CRM checkpoint has no remaining code-level blocker. Profile, tag, and pipeline-definition browser gates become required when complete frontend consumers are introduced. Public forms and CSV transfer remain HTTP behind their hardened retained-protocol boundaries, all GraphQL flags remain off in deployed builds, and no aggregate-profile frontend cutover is required until a real consumer exists.

## Per-slice exit gate

A slice is ready to cut over only when all of the following are true:

- its ledger row has an explicit disposition and owner
- auth and tenant-denial cases pass
- success, validation, not-found, conflict, and failure paths are characterized
- database state and external side effects match the intended semantics
- retries are safe for mutations that can be repeated
- the frontend or external consumer uses the new operation successfully
- metrics/logs distinguish legacy and GraphQL execution
- rollback does not require data repair

The Express backend can be retired only after all externally used ledger rows are migrated, deliberately retained, or deliberately removed and the observation window shows no legacy traffic.
